.content
  .row
    .col-lg-8.col-lg-offset-2
      .widget: .well
        form.form-horizontal(
        name='loginForm', method='post'): fieldset

          legend
            span.fa.fa-sign-in
            | Login

          // Username
          .form-group
            label.col-md-3.control-label(for="username") Username
            .col-md-9
              input.form-control(type="text" ng-model="user.username" required
              ng-focus="usernameFocus=true" ng-blur="usernameFocus=false"
              name="username" placeholder="Username")

          // Password
          .form-group
            label.col-md-3.control-label(for="password") Password
            .col-md-9
              input.form-control(type="password" ng-model="user.password" required
              ng-focus="passwordFocus=true" ng-blur="passwordFocus=false"
              name="password" placeholder="Password")

          // Submit
          .form-group
            .col-lg-9.col-lg-offset-3.clearfix
              button.btn.btn-primary.pull-right(
              ng-click='ctrl.login(user, loginForm)'
              ng-disabled='loginForm.$invalid') Submit

        hr
        p.text-right #{'Need an account? '}
          a(href='/register') Signup